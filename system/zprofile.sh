#? Init
# Global overall functionality, which loads in user's .zprofile, to make sure that all the necessary functions are available for the user from start

# source "$STS_DIR/system/zprofile/stsSource.sh"
